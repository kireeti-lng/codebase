# codebase


Markdown # BigQuery CLI (bq) Command Cheat Sheet This guide covers essential commands for executing SQL queries using the Google Cloud SDK. \\*Note: Always use --use\\\_legacy\\\_sql=false (or --nouse\\\_legacy\\\_sql) to ensure BigQuery processes your query using Standard SQL.\\* ## 1. Run a Basic Inline QueryExecute a quick query directly from your terminal. ```bashbq query --use\\\_legacy\\\_sql=false 'SELECT event\\\_id, event\\\_name, event\\\_date FROM your-project.your\\\_dataset.events\\\_raw LIMIT 10' ## 2. Run a Query from a .sql File Execute a query saved in a local file. \\*\\*Method A: Input Redirection\\*\\* Bash bq query --use\\\_legacy\\\_sql=false &lt; my\\\_query.sql \\*\\*Method B: Piping\\*\\* Bash cat my\\\_query.sql | bq query --use\\\_legacy\\\_sql=false ## 3. Save Results to a Destination Table Materialize the results of your query into a new or existing BigQuery table.\\*Format: \\*\\*project\\\_id:dataset.table\\\_name\\* Bash bq query --use\\\_legacy\\\_sql=false --destination\\\_table='your-project:your\\\_dataset.new\\\_summary\\\_table' &lt;span class="hljs-string"&gt;'SELECT event\\\_date, COUNT(event\\\_id) as event\\\_count FROM your-project.your\\\_dataset.events\\\_raw GROUP BY event\\\_date' ## 4. Export Results Locally Save the query output to a local file in your preferred format. \\*\\*Export as CSV:\\*\\* Bash bq query --use\\\_legacy\\\_sql=false --format=csv --max\\\_rows=1000 &lt;span class="hljs-string"&gt;'SELECT \\\* FROM your-project.your\\\_dataset.events\\\_raw' &gt; output.csv \\*\\*Export as JSON:\\*\\* Bash bq query --use\\\_legacy\\\_sql=false --format=prettyjson &lt;span class="hljs-string"&gt;'SELECT \\\* FROM your-project.your\\\_dataset.events\\\_raw LIMIT 10' &gt; output.json ## 5. Cost Control (Limit Bytes Billed) Prevent accidental large bills by capping the amount of data your query is allowed to scan. If the query exceeds this limit, it will fail before executing. Bash bq query --use\\\_legacy\\\_sql=false --maximum\\\_bytes\\\_billed=1000000000 &lt;span class="hljs-string"&gt;'SELECT \\\* FROM your-project.your\\\_dataset.events\\\_raw'
